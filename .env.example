# Signal Account Number (e.g. +1234567890)
# You can find this by running `python setup_signal.py` or `signal-cli listAccounts`
SIGNAL_ACCOUNT=

# Anytype API Configuration
# Enable Local API in Anytype Desktop -> Settings -> Advanced -> Local API
# Generate a key and paste it here
ANYTYPE_API_KEY=
ANYTYPE_API_URL=http://localhost:31001/v1

# Claude API Key (Optional)
# If provided, the bot will use Claude to process messages intelligently
ANTHROPIC_API_KEY=
